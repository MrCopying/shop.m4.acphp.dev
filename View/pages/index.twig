{#<?php foreach ($data['pages'] as $page_data) {?>#}

    {#<div style="margin-top: 20px;">#}
        {#<a href="/pages/view/<?= $page_data['alias']?>"><?= $page_data['title']?></a>#}
    {#</div>#}
{#<?php } ?>#}

{% for page in pages %}
<div>
    <a href="
    {% if page.alias %}/pages/view/{{ page.alias }}
    {% else %}{{ page.uri }}
    {% endif %}
    ">{{ page.name }}</a>
</div>
{% endfor %}